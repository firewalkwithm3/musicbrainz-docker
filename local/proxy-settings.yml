services:
  musicbrainz:
    ports:
    networks:
      - default
      - proxy
    labels:
      caddy: musicbrainz.ferngarden.net
      caddy.import: internal
      caddy.reverse_proxy: '{{ upstreams 5000 }}'

networks:
  proxy:
    external: true
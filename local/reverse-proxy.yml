services:
  musicbrainz:
    networks:
      - default
      - proxy
    labels:
      caddy: musicbrainz.ferngarden.net
      caddy.import: internal
      caddy.reverse_proxy: musicbrainz:80

networks:
  default:
  proxy:
    external: true